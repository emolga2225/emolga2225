# Core dependencies
torch>=2.0.0
numpy>=1.24.0
scipy>=1.10.0
h5py>=3.8.0
tqdm>=4.65.0

# Audio processing
soundfile>=0.12.0

# NSF/Game music support
# libgme is NOT a pip package. Download libgme.dll (Windows) or install via:
#   Linux:  sudo apt install libgme-dev
#   Mac:    brew install game-music-emu
#   Windows: https://github.com/ShiftMediaProject/game-music-emu/releases

# Optional: GPU acceleration for synthesis
# cupy>=12.0.0  # Uncomment if using --gpu flag
